import type { NextConfig } from 'next';
import { experimental_ppr } from './app/dashboard/layout';

const nextConfig: NextConfig = {
  /* config options here */
  // experimental:{
  //   ppr: 'incremental',
  // },
 
};

export default nextConfig;
